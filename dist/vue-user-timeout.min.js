(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else if(typeof exports === 'object')
		exports["VueUserTimeout"] = factory();
	else
		root["VueUserTimeout"] = factory();
})(window, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./src/lib/userTimeout.js
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(source, true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(source).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

/* harmony default export */ var lib_userTimeout = (function (Vue) {
  return new Vue({
    data: function data() {
      return {
        mergedOptions: null,
        timerInterval: null,
        startTime: null,
        currentTime: null,
        elapsedTime: null,
        isActive: false,
        isInitialized: false
      };
    },
    methods: {
      init: function init(options) {
        var _this = this;

        var defaultOptions = {
          timeout: 60000,
          // 10 min
          updateInterval: 500,
          // .5 sec
          events: ['resize', 'scroll', 'keydown', 'mousemove'],
          startOnload: false,
          destroyOnTimeout: true
        };
        this.mergedOptions = _objectSpread({}, defaultOptions, {}, options); // eslint-disable-next-line no-underscore-dangle

        this._addListeners().then(function () {
          _this.$emit('timeout-initialized'); // start the time


          _this.isInitialized = true;

          if (_this.mergedOptions.startOnload) {
            _this.start();
          }
        })["catch"](function () {
          console.log('There was an error adding event listeners');
        });
      },
      start: function start() {
        var _this2 = this;

        console.log(this); // prevent start being called while it's already running

        if (this.isActive || !this.isInitialized) {
          console.log('invalid start');
          return;
        }

        if (this.startTime === null) {
          this.startTime = Date.now();
        }

        this.isActive = true;
        this.timerInterval = setInterval(function () {
          _this2.currentTime = Date.now();
          _this2.elapsedTime = _this2.currentTime - _this2.startTime; // console.log(`tick tick ${(this.elapsedTime / 1000).toFixed(3)}`);

          if (_this2.elapsedTime >= _this2.mergedOptions.timeout) {
            // emit event
            _this2.$emit('timeout-completed');

            _this2.stop();

            if (_this2.mergedOptions.destroyOnTimeout) {
              _this2.destroy();
            }
          }
        }, this.mergedOptions.updateInterval);
        this.$emit('timeout-started');
      },
      reset: function reset() {
        if (!this.isActive && !this.isInitialized) {
          return;
        }

        this.stop();

        if (!this.mergedOptions.destroyOnTimeout) {
          this.start();
        }

        this.$emit('timeout-reset');
      },
      stop: function stop() {
        if (!this.isActive && !this.isInitialized) {
          return;
        }

        clearInterval(this.timerInterval);
        this.startTime = null;
        this.isActive = false;
        this.$emit('timeout-stopped');
      },
      pause: function pause() {
        if (!this.isActive && !this.isInitialized) {
          return;
        }

        clearInterval(this.timerInterval);
        this.isActive = false;
        this.$emit('timeout-paused');
      },
      destroy: function destroy() {
        var _this3 = this;

        return new Promise(function (resolve, reject) {
          if (!_this3.isInitialized) {
            reject();
          }

          try {
            if (_this3.isActive) {
              _this3.stop();
            }

            _this3.mergedOptions.events.forEach(function (evnt) {
              document.removeEventListener(evnt, _this3.reset);
            });

            _this3.mergedOptions = null;
            _this3.isInitialized = false;

            _this3.$emit('timeout-destroyed');

            resolve();
          } catch (err) {
            reject(err);
          }
        });
      },
      // eslint-disable-next-line no-underscore-dangle
      _addListeners: function _addListeners() {
        var _this4 = this;

        return new Promise(function (resolve, reject) {
          try {
            _this4.mergedOptions.events.forEach(function (evnt) {
              document.addEventListener(evnt, _this4.reset);
            });

            resolve();
          } catch (err) {
            reject(err);
          }
        });
      }
    }
  });
});
// CONCATENATED MODULE: ./src/index.js

var VueUserTimeout = {
  // eslint-disable-next-line no-unused-vars
  install: function install(Vue) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    // console.log('VueUserTimeout Installed');
    var userTimeout = lib_userTimeout(Vue);
    userTimeout.init(options); // eslint-disable-next-line no-param-reassign

    Vue.prototype.$userTimeout = userTimeout; // eslint-disable-next-line no-param-reassign

    Vue.$userTimeout = userTimeout;
  }
}; // Logic to determine how to include the plugin

if (window.Vue && typeof window !== 'undefined') {
  // eslint-disable-next-line no-undef
  window.Vue.use(VueUserTimeout);
}

/* harmony default export */ var src = __webpack_exports__["default"] = (VueUserTimeout);

/***/ })
/******/ ]);
});